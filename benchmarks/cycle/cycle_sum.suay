⍝ cycle-heavy: explicit state-machine summation

sum_to ← ⌁(n)
  ⟲ (Step•(1 0)) ▷ ⟪
  ▷ Done•acc     ⇒ ↯ acc
  ▷ Step•(i acc) ⇒ ↩ (
        (i > n) ▷ ⟪
        ▷ ⊤ ⇒ Done•acc
        ▷ ⊥ ⇒ Step•(i + 1  acc + i)
        ⟫
      )
  ⟫

sum_to · 5000
