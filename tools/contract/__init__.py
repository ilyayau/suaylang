"""V1 core contract test runner.

This package runs the contract corpus in `tests/contract/` and validates that
both the interpreter and VM conform to a stable, reviewer-friendly semantics.

Run:
  python -m tools.contract
"""
