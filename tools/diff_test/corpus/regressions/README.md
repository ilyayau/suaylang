Minimized counterexamples.

- `*.suay` are minimized reproducer programs.
- `*.json` stores the divergence metadata and observations.

Files in this directory are *automatically generated* by `tools/diff_test/minimizer.py`.
They should be treated as regression tests: keep them checked in.
