# Baseline suite for SuayLang baseline_runner
# This directory contains a fixed set of programs for baseline comparison.
# Each .suay file is a small, deterministic program.
# Each .py file is a Python equivalent (where possible).
