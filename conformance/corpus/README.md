# Conformance corpus

This directory contains a fixed, human-readable program corpus used for interpreter↔VM equivalence checks.

Run:

```sh
python tools/conformance/run.py conformance/corpus
```

Program metadata (construct coverage) is in:

- [conformance/manifest.json](../manifest.json)
