# Program Equivalence

Two runs are equivalent if:
- They produce the same value, OR
- They produce the same (error_code, span category)

Intentionally ignored:
- Message text
- Formatting
- Non-deterministic output
