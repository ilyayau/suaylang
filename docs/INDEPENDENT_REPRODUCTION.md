# Independent Reproduction Log

## Reproduction Kit
- Clone: `git clone <repo-url>`
- Setup: `python -m venv .venv && source .venv/bin/activate && python -m pip install -e .`
- Run: `make research && make tr`
- Artifacts: `results/` (all .md/.json), `paper/TR-2025-01.pdf`

## Template for Reproducer
- Name/Handle:
- OS/CPU:
- Date:
- Command run:
- Outcome:
- “No author intervention”: [ ]
- Comments:

(If a real reproduction exists, fill this log with details.)
