# Authors

Primary maintainer / corresponding author:

- ilyayau (GitHub handle)

## Authorship / optics note

Some commits may show a different local git author identity due to local machine configuration.

Project ownership and ongoing maintenance are by the primary maintainer listed above.

### Recommended git config (future commits)

```sh
git config --global user.name "Your Name"
git config --global user.email "you@example.com"

git config --global --list | grep -E "user\\.(name|email)"
```

If you contribute via multiple machines, set these consistently on each machine.
